
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        .cdf-header {
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
        }
        .cdf-footer {
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
        }
        .cdf-logo {
            color: #ef4444;
            font-weight: 700;
            font-size: 1.5rem;
        }
        .cdf-button-primary {
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
            color: white;
            border: none;
            border-radius: 8px;
            padding: 12px 24px;
            font-weight: 600;
            transition: all 0.3s ease;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        .cdf-button-primary:hover {
            background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
            transform: translateY(-1px);
            box-shadow: 0 8px 25px rgba(239, 68, 68, 0.3);
        }
        .cdf-button-secondary {
            background: transparent;
            color: #ef4444;
            border: 2px solid #ef4444;
            border-radius: 8px;
            padding: 10px 22px;
            font-weight: 600;
            transition: all 0.3s ease;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        .cdf-button-secondary:hover {
            background: #ef4444;
            color: white;
            transform: translateY(-1px);
        }
        .cdf-button-dark {
            background: linear-gradient(135deg, #374151 0%, #1f2937 100%);
            color: white;
            border: none;
            border-radius: 8px;
            padding: 12px 24px;
            font-weight: 600;
            transition: all 0.3s ease;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        .cdf-button-dark:hover {
            background: linear-gradient(135deg, #1f2937 0%, #111827 100%);
            transform: translateY(-1px);
        }
        .cdf-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            transition: all 0.3s ease;
        }
        .nav-link {
            color: #d1d5db;
            transition: all 0.3s ease;
            padding: 8px 16px;
            border-radius: 6px;
        }
        .nav-link:hover {
            color: #ef4444;
            background: rgba(239, 68, 68, 0.1);
        }
        .nav-link.active {
            color: #ef4444;
            background: rgba(239, 68, 68, 0.15);
        }
        .data-grid {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }
        .grid-header {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            padding: 16px;
            border-bottom: 1px solid #e2e8f0;
        }
        .grid-row {
            border-bottom: 1px solid #f1f5f9;
            transition: all 0.2s ease;
        }
        .grid-row:hover {
            background: #f8fafc;
        }
        .status-badge {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }
        .status-active {
            background: #dcfce7;
            color: #166534;
        }
        .status-inactive {
            background: #fef2f2;
            color: #991b1b;
        }
        .status-pending {
            background: #fef3c7;
            color: #92400e;
        }
        .revenue-card {
            background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <!-- Header -->
    <header class="cdf-header shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center space-x-4">
                    <div class="cdf-logo flex items-center">
                        <i class="fas fa-dumbbell mr-2"></i>
                        CDF
                    </div>
                    <span class="text-white text-sm font-medium">Entrenamiento Elite</span>
                    <div class="flex items-center space-x-2 ml-6">
                        <i class="fas fa-database text-gray-400 text-sm"></i>
                        <span class="text-gray-300 text-sm">DB</span>
                    </div>
                </div>
                
                <nav class="flex items-center space-x-6">
                    <a href="/alumnos" class="nav-link active">
                        <i class="fas fa-users mr-1"></i>Alumnos
                    </a>
                    <a href="/planes" class="nav-link">
                        <i class="fas fa-clipboard-list mr-1"></i>Planes
                    </a>
                    <a href="/colaboradores" class="nav-link">
                        <i class="fas fa-user-tie mr-1"></i>Colaboradores
                    </a>
                    <a href="/logout" class="cdf-button-primary ml-4">
                        Cerrar sesión
                    </a>
                </nav>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1 max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8 w-full">
        <!-- Page Header -->
        <div class="flex justify-between items-center mb-8">
            <div>
                <h1 class="text-3xl font-bold text-gray-900">Gestión de Alumnos</h1>
                <p class="mt-2 text-gray-600">Administra los miembros del gimnasio CDF</p>
            </div>
            <a href="/alumnos/add" class="cdf-button-primary">
                <i class="fas fa-plus"></i>
                Nuevo Alumno
            </a>
        </div>

        <!-- Revenue Summary -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
            <div class="cdf-card revenue-card p-6">
                <div class="flex items-center justify-between mb-4">
                    <h3 class="text-lg font-semibold text-gray-900">Recaudación Mensual</h3>
                    <button onclick="toggleVisibility('monthly')" class="text-gray-500 hover:text-gray-700">
                        <i id="monthly-icon" class="fas fa-eye-slash"></i>
                    </button>
                </div>
                <div class="space-y-2">
                    <div class="flex items-center justify-between">
                        <span class="text-sm text-gray-600">Total recaudado este mes</span>
                        <span id="monthly-amount" class="text-2xl font-bold text-blue-600">$ ***</span>
                    </div>
                    <div class="flex items-center justify-between">
                        <span class="text-sm text-gray-600">Membresías pagadas</span>
                        <span class="text-sm font-medium text-gray-900"><%= membresiasPagadas %></span>
                    </div>
                </div>
                <div class="hidden" id="monthly-real">$<%= recaudacionMensual.toLocaleString('es-AR') %></div>
            </div>

            <div class="cdf-card p-6">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">Estadísticas Generales</h3>
                <div class="space-y-3">
                    <div class="flex items-center justify-between">
                        <span class="text-sm text-gray-600">Total de alumnos</span>
                        <span class="text-lg font-bold text-gray-900"><%= membresiasTotales %></span>
                    </div>
                    <div class="flex items-center justify-between">
                        <span class="text-sm text-gray-600">Ingresos potenciales</span>
                        <button onclick="toggleVisibility('potential')" class="flex items-center space-x-2">
                            <span id="potential-amount" class="text-lg font-bold text-green-600">$ ***</span>
                            <i id="potential-icon" class="fas fa-eye-slash text-gray-500"></i>
                        </button>
                    </div>
                    <div class="hidden" id="potential-real">$<%= ingresosPotenciales.toLocaleString('es-AR') %></div>
                </div>
            </div>
        </div>

        <!-- Data Grid -->
        <div class="data-grid">
            <div class="grid-header">
                <h3 class="text-lg font-semibold text-gray-900">Lista de Alumnos</h3>
                <p class="text-sm text-gray-600 mt-1">Gestiona la información de todos los miembros</p>
            </div>
            
            <div class="p-6">
                <div id="studentsContainer">
                    <div class="flex items-center justify-center py-12">
                        <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-red-500"></div>
                        <span class="ml-3 text-gray-600">Cargando alumnos...</span>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="cdf-footer mt-auto">
        <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center">
                <div class="flex items-center space-x-4">
                    <span class="text-gray-300 text-sm">Desarrollado por Alan Stefanov, Software Engineer</span>
                </div>
                <div class="flex items-center space-x-4">
                    <div class="cdf-logo">CDF</div>
                    <span class="text-gray-300 text-sm">©2025 CDF Entrenamiento Elite. Todos los derechos reservados</span>
                </div>
            </div>
        </div>
    </footer>

    <script>
        function toggleVisibility(type) {
            const icon = document.getElementById(type + '-icon');
            const amount = document.getElementById(type + '-amount');
            const real = document.getElementById(type + '-real');
            
            if (icon.classList.contains('fa-eye-slash')) {
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
                amount.textContent = real.textContent;
            } else {
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
                amount.textContent = '$ ***';
            }
        }

        function deleteAlumno(id, nombre) {
            if (confirm(`¿Estás seguro de eliminar al alumno ${nombre}?`)) {
                fetch(`/alumnos/${id}`, {
                    method: 'DELETE',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        loadStudents();
                    } else {
                        alert('Error al eliminar el alumno');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('Error al eliminar el alumno');
                });
            }
        }

        function getStatusBadge(status) {
            const statusMap = {
                'Activa': { class: 'status-active', text: 'Activa' },
                'Inactiva': { class: 'status-inactive', text: 'Inactiva' },
                'Vencida': { class: 'status-inactive', text: 'Vencida' },
                'Pendiente': { class: 'status-pending', text: 'Pendiente' }
            };
            
            const statusInfo = statusMap[status] || { class: 'status-pending', text: status };
            return `<span class="status-badge ${statusInfo.class}">${statusInfo.text}</span>`;
        }

        function loadStudents() {
            console.log('DOM loaded, loading students...');
            fetch('/alumnos/api')
                .then(response => response.json())
                .then(data => {
                    console.log('Data received:', data);
                    const container = document.getElementById('studentsContainer');
                    
                    if (data.length === 0) {
                        container.innerHTML = `
                            <div class="text-center py-12">
                                <div class="w-16 h-16 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
                                    <i class="fas fa-users text-gray-400 text-2xl"></i>
                                </div>
                                <h3 class="text-lg font-medium text-gray-900 mb-2">No hay alumnos registrados</h3>
                                <p class="text-gray-600 mb-4">Comienza agregando el primer alumno al sistema</p>
                                <a href="/alumnos/add" class="cdf-button-primary">
                                    <i class="fas fa-plus"></i>
                                    Agregar Primer Alumno
                                </a>
                            </div>
                        `;
                        return;
                    }
                    
                    let html = `
                        <div class="overflow-x-auto">
                            <table class="min-w-full">
                                <thead>
                                    <tr class="border-b border-gray-200">
                                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Alumno</th>
                                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Contacto</th>
                                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Plan</th>
                                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Estado</th>
                                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Vencimiento</th>
                                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                    `;
                    
                    data.forEach(alumno => {
                        const fechaVencimiento = new Date(alumno.fecha_vencimiento_membresia);
                        const fechaFormateada = fechaVencimiento.toLocaleDateString('es-ES');
                        
                        html += `
                            <tr class="grid-row">
                                <td class="px-6 py-4">
                                    <div>
                                        <div class="text-sm font-medium text-gray-900">${alumno.nombre} ${alumno.apellido}</div>
                                        <div class="text-sm text-gray-500">DNI: ${alumno.dni}</div>
                                    </div>
                                </td>
                                <td class="px-6 py-4">
                                    <div class="text-sm text-gray-900">${alumno.email}</div>
                                    <div class="text-sm text-gray-500">${alumno.telefono}</div>
                                </td>
                                <td class="px-6 py-4">
                                    <div class="text-sm text-gray-900">${alumno.plan ? alumno.plan.nombre_plan : 'Sin plan'}</div>
                                </td>
                                <td class="px-6 py-4">
                                    ${getStatusBadge(alumno.estado_membresia)}
                                </td>
                                <td class="px-6 py-4">
                                    <div class="text-sm text-gray-900">${fechaFormateada}</div>
                                </td>
                                <td class="px-6 py-4">
                                    <div class="flex space-x-2">
                                        <a href="/alumnos/edit/${alumno.id}" class="text-blue-600 hover:text-blue-800">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <button onclick="deleteAlumno(${alumno.id}, '${alumno.nombre} ${alumno.apellido}')" class="text-red-600 hover:text-red-800">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        `;
                    });
                    
                    html += `
                                </tbody>
                            </table>
                        </div>
                    `;
                    
                    container.innerHTML = html;
                })
                .catch(error => {
                    console.error('Error:', error);
                    document.getElementById('studentsContainer').innerHTML = `
                        <div class="text-center py-12">
                            <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
                                <i class="fas fa-exclamation-triangle text-red-500 text-2xl"></i>
                            </div>
                            <h3 class="text-lg font-medium text-gray-900 mb-2">Error al cargar alumnos</h3>
                            <p class="text-gray-600 mb-4">No se pudieron cargar los datos de los alumnos</p>
                            <button onclick="loadStudents()" class="cdf-button-secondary">
                                <i class="fas fa-refresh"></i>
                                Reintentar
                            </button>
                        </div>
                    `;
                });
        }

        // Load students when page loads
        document.addEventListener('DOMContentLoaded', loadStudents);
    </script>
</body>
</html>
