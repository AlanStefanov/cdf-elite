
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .material-card {
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            transition: box-shadow 0.3s ease;
        }
        .material-card:hover {
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }
        .stat-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 12px;
            padding: 24px;
            text-align: center;
            transition: transform 0.3s ease;
        }
        .stat-card:hover {
            transform: translateY(-2px);
        }
        .stat-number {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 8px;
        }
        .stat-label {
            font-size: 1rem;
            opacity: 0.9;
        }
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 10px;
            color: white;
            padding: 12px 24px;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }
        .alert {
            padding: 12px 16px;
            border-radius: 4px;
            margin-bottom: 16px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .alert-error {
            background-color: #fef2f2;
            border: 1px solid #fecaca;
            color: #dc2626;
        }
        .alert-success {
            background-color: #f0fdf4;
            border: 1px solid #bbf7d0;
            color: #16a34a;
        }
        .role-badge {
            display: inline-flex;
            align-items: center;
            px-2 py-1;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 500;
        }
        .role-entrenador {
            background-color: #dbeafe;
            color: #1e40af;
        }
        .role-administrador {
            background-color: #fef3c7;
            color: #92400e;
        }
        .role-recepcionista {
            background-color: #d1fae5;
            color: #065f46;
        }
        .role-default {
            background-color: #f3f4f6;
            color: #374151;
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Navigation -->
    <nav class="gradient-bg shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <a href="/dashboard" class="text-white text-xl font-bold">
                        <i class="fas fa-dumbbell mr-2"></i>
                        CDF Entrenamiento Elite
                    </a>
                </div>
                <div class="flex items-center space-x-6">
                    <a href="/dashboard" class="text-white hover:text-gray-200 transition-colors font-semibold">
                        <i class="fas fa-chart-bar mr-1"></i>Dashboard
                    </a>
                    <a href="/alumnos" class="text-white hover:text-gray-200 transition-colors">
                        <i class="fas fa-users mr-1"></i>Alumnos
                    </a>
                    <a href="/planes" class="text-white hover:text-gray-200 transition-colors">
                        <i class="fas fa-clipboard-list mr-1"></i>Planes
                    </a>
                    <a href="/colaboradores" class="text-white hover:text-gray-200 transition-colors">
                        <i class="fas fa-user-tie mr-1"></i>Colaboradores
                    </a>
                    <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg transition-colors">
                        <i class="fas fa-sign-out-alt mr-1"></i>Cerrar Sesión
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="flex-1 max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
        <!-- Page Header -->
        <div class="flex justify-between items-center mb-8">
            <div>
                <h1 class="text-3xl font-bold text-gray-900">Gestión de Colaboradores</h1>
                <p class="mt-2 text-gray-600">Administra el equipo de trabajo del gimnasio</p>
            </div>
            <a href="/colaboradores/add" class="material-button">
                <span class="material-icons text-sm">person_add</span>
                Agregar Colaborador
            </a>
        </div>

        <!-- Flash Messages -->
        <% if (typeof messages !== 'undefined') { %>
            <% if (messages.error && messages.error.length > 0) { %>
                <div class="alert alert-error">
                    <span class="material-icons">error</span>
                    <%= messages.error[0] %>
                </div>
            <% } %>
            <% if (messages.success && messages.success.length > 0) { %>
                <div class="alert alert-success">
                    <span class="material-icons">check_circle</span>
                    <%= messages.success[0] %>
                </div>
            <% } %>
        <% } %>

        <!-- Colaboradores Table -->
        <div class="bg-white rounded-lg shadow overflow-hidden">
            <div class="px-6 py-4 border-b border-gray-200">
                <div class="flex items-center justify-between">
                    <h3 class="text-lg font-medium text-gray-900">Lista de Colaboradores</h3>
                    <div class="flex items-center space-x-3">
                        <div class="relative">
                            <input type="text" id="searchInput" placeholder="Buscar..." class="w-64 px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                            <i class="fas fa-search absolute right-3 top-2.5 text-gray-400"></i>
                        </div>
                    </div>
                </div>
            </div>
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Colaborador</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Rol</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Correo</th>
                            <th class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Acciones</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <% if (colaboradores && colaboradores.length > 0) { %>
                            <% colaboradores.forEach(colaborador => { %>
                            <tr class="hover:bg-gray-50">
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 h-10 w-10">
                                            <div class="h-10 w-10 rounded-full bg-blue-100 flex items-center justify-center">
                                                <i class="fas fa-user text-blue-600"></i>
                                            </div>
                                        </div>
                                        <div class="ml-4">
                                            <div class="text-sm font-medium text-gray-900"><%= colaborador.nombre %> <%= colaborador.apellido %></div>
                                        </div>
                                    </div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <span class="role-badge role-<%= colaborador.rol.toLowerCase() %>">
                                        <%= colaborador.rol %>
                                    </span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="text-sm text-gray-900"><%= colaborador.email %></div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                                    <a href="/colaboradores/edit/<%= colaborador.id %>">
                                        <button class="btn-primary bg-blue-500 hover:bg-blue-600 text-white">
                                            <i class="fas fa-edit mr-2"></i>
                                            Editar
                                        </button>
                                    </a>
                                    <button class="btn-primary bg-red-500 hover:bg-red-600 text-white" onclick="confirmDelete('<%= colaborador.id %>')">
                                        <i class="fas fa-trash mr-2"></i>
                                        Eliminar
                                    </button>
                                </td>
                            </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="4" class="px-6 py-4 text-center text-sm text-gray-500">
                                    No hay colaboradores registrados
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t mt-8">
        <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
            <div class="text-center text-gray-500 text-sm">
                &copy; 2024 CDF Entrenamiento Elite. Todos los derechos reservados.
            </div>
            <div class="text-center text-gray-500 text-sm mt-1">
                Desarrollado por Alan Stefanov, Software Engineer.
            </div>
        </div>
    </footer>
</body>
</html>
