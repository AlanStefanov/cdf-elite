
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }
        .material-button {
            background: linear-gradient(45deg, #3b82f6, #1d4ed8);
            color: white;
            border: none;
            border-radius: 4px;
            padding: 12px 24px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            text-decoration: none;
        }
        .material-button:hover {
            background: linear-gradient(45deg, #1d4ed8, #1e40af);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <!-- Header -->
    <header class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <img src="/images/logo.jpg" alt="CDF Logo" class="h-10 w-10 rounded-full mr-3">
                    <h1 class="text-xl font-semibold text-gray-900">CDF Entrenamiento Elite</h1>
                </div>
                <% if (typeof user !== 'undefined' && user) { %>
                <nav class="flex space-x-6">
                    <a href="/dashboard" class="text-gray-600 hover:text-gray-900">Dashboard</a>
                    <a href="/alumnos" class="text-gray-600 hover:text-gray-900">Alumnos</a>
                    <a href="/planes" class="text-gray-600 hover:text-gray-900">Planes</a>
                    <a href="/colaboradores" class="text-gray-600 hover:text-gray-900">Colaboradores</a>
                    <a href="/logout" class="text-red-600 hover:text-red-700">Cerrar Sesión</a>
                </nav>
                <% } %>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1 flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full text-center">
            <div class="mb-8">
                <span class="material-icons text-gray-400 text-9xl">error_outline</span>
            </div>
            
            <h1 class="text-6xl font-bold text-gray-900 mb-4">404</h1>
            <h2 class="text-3xl font-semibold text-gray-700 mb-4">Página no encontrada</h2>
            <p class="text-gray-600 mb-8">
                Lo sentimos, la página que estás buscando no existe o ha sido movida.
            </p>
            
            <div class="space-y-4">
                <% if (typeof user !== 'undefined' && user) { %>
                    <a href="/dashboard" class="material-button w-full justify-center">
                        <span class="material-icons text-sm">home</span>
                        Ir al Dashboard
                    </a>
                <% } else { %>
                    <a href="/login" class="material-button w-full justify-center">
                        <span class="material-icons text-sm">login</span>
                        Ir al Login
                    </a>
                <% } %>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t mt-auto">
        <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
            <div class="text-center">
                <p class="text-sm text-gray-600">
                    © 2025 CDF Entrenamiento Elite. Todos los derechos reservados.
                </p>
                <p class="text-sm text-gray-500 mt-1">
                    Desarrollado por Alan Stefanov, Software Engineer.
                </p>
            </div>
        </div>
    </footer>
</body>
</html>
