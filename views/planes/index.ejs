
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }
        .material-card {
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            transition: box-shadow 0.3s ease;
        }
        .material-card:hover {
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }
        .material-button {
            background: linear-gradient(45deg, #3b82f6, #1d4ed8);
            color: white;
            border: none;
            border-radius: 4px;
            padding: 8px 16px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            display: inline-flex;
            align-items: center;
            gap: 4px;
        }
        .material-button:hover {
            background: linear-gradient(45deg, #1d4ed8, #1e40af);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
        }
        .material-button.secondary {
            background: linear-gradient(45deg, #6b7280, #4b5563);
        }
        .material-button.secondary:hover {
            background: linear-gradient(45deg, #4b5563, #374151);
        }
        .material-button.danger {
            background: linear-gradient(45deg, #ef4444, #dc2626);
        }
        .material-button.danger:hover {
            background: linear-gradient(45deg, #dc2626, #b91c1c);
        }
        .alert {
            padding: 12px 16px;
            border-radius: 4px;
            margin-bottom: 16px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .alert-error {
            background-color: #fef2f2;
            border: 1px solid #fecaca;
            color: #dc2626;
        }
        .alert-success {
            background-color: #f0fdf4;
            border: 1px solid #bbf7d0;
            color: #16a34a;
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <!-- Header -->
    <header class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <img src="/images/logo.jpg" alt="CDF Logo" class="h-10 w-10 rounded-full mr-3">
                    <h1 class="text-xl font-semibold text-gray-900">CDF Entrenamiento Elite</h1>
                </div>
                <nav class="flex space-x-6">
                    <a href="/dashboard" class="text-gray-600 hover:text-gray-900">Dashboard</a>
                    <a href="/alumnos" class="text-gray-600 hover:text-gray-900">Alumnos</a>
                    <a href="/planes" class="text-blue-600 font-medium">Planes</a>
                    <a href="/colaboradores" class="text-gray-600 hover:text-gray-900">Colaboradores</a>
                    <a href="/logout" class="text-red-600 hover:text-red-700">Cerrar Sesión</a>
                </nav>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1 max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
        <!-- Page Header -->
        <div class="flex justify-between items-center mb-8">
            <div>
                <h1 class="text-3xl font-bold text-gray-900">Gestión de Planes</h1>
                <p class="mt-2 text-gray-600">Administra los planes de entrenamiento disponibles</p>
            </div>
            <a href="/planes/add" class="material-button">
                <span class="material-icons text-sm">add</span>
                Agregar Plan
            </a>
        </div>

        <!-- Flash Messages -->
        <% if (typeof messages !== 'undefined') { %>
            <% if (messages.error && messages.error.length > 0) { %>
                <div class="alert alert-error">
                    <span class="material-icons">error</span>
                    <%= messages.error[0] %>
                </div>
            <% } %>
            <% if (messages.success && messages.success.length > 0) { %>
                <div class="alert alert-success">
                    <span class="material-icons">check_circle</span>
                    <%= messages.success[0] %>
                </div>
            <% } %>
        <% } %>

        <!-- Plans Table -->
        <div class="material-card overflow-hidden">
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Plan</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Precio</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Duración</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Descripción</th>
                            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Acciones</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <% if (planes && planes.length > 0) { %>
                            <% planes.forEach(plan => { %>
                            <tr class="hover:bg-gray-50">
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 h-10 w-10">
                                            <div class="h-10 w-10 rounded-full bg-blue-100 flex items-center justify-center">
                                                <span class="material-icons text-blue-600">fitness_center</span>
                                            </div>
                                        </div>
                                        <div class="ml-4">
                                            <div class="text-sm font-medium text-gray-900"><%= plan.nombre_plan %></div>
                                        </div>
                                    </div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="text-sm text-gray-900 font-semibold">$<%= plan.precio %></div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="text-sm text-gray-900"><%= plan.duracion_dias %> días</div>
                                </td>
                                <td class="px-6 py-4">
                                    <div class="text-sm text-gray-900 max-w-xs truncate"><%= plan.descripcion %></div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                                    <div class="flex justify-end space-x-2">
                                        <a href="/planes/edit/<%= plan.id %>" class="material-button secondary">
                                            <span class="material-icons text-sm">edit</span>
                                            Editar
                                        </a>
                                        <form action="/planes/delete/<%= plan.id %>" method="POST" class="inline" onsubmit="return confirm('¿Estás seguro de que quieres eliminar este plan?')">
                                            <button type="submit" class="material-button danger">
                                                <span class="material-icons text-sm">delete</span>
                                                Eliminar
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="5" class="px-6 py-12 text-center">
                                    <div class="flex flex-col items-center">
                                        <span class="material-icons text-gray-400 text-5xl mb-4">fitness_center</span>
                                        <h3 class="text-lg font-medium text-gray-900 mb-2">No hay planes registrados</h3>
                                        <p class="text-gray-500 mb-4">Comienza agregando tu primer plan de entrenamiento.</p>
                                        <a href="/planes/add" class="material-button">
                                            <span class="material-icons text-sm">add</span>
                                            Agregar Primer Plan
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t mt-auto">
        <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
            <div class="text-center">
                <p class="text-sm text-gray-600">
                    © 2025 CDF Entrenamiento Elite. Todos los derechos reservados.
                </p>
                <p class="text-sm text-gray-500 mt-1">
                    Desarrollado por Alan Stefanov, Software Engineer.
                </p>
            </div>
        </div>
    </footer>
</body>
</html>
